import { atom, map } from 'nanostores';

export const $isMenuOpen = atom<boolean>(false);
// export const isHeaderDark = atom<boolean>(false);

export const $isPrivacyMenuOpen = atom<boolean>(false);


